def crearLista():
    lista=[]
    n=int(input("Cuantos elementos tiene la lista?"))
    for x in range(n):
          valor=input("Introduce un elemento de la lista:")
          lista.appeand(valor)
    return lista

#función que multiplica los elementos de una lista por un valor
def mascaracteres(lista):
    palabra=lista[0]
        for x in range(1,len(lista)):
            if len(lista[x]>len(palabras):
                   palabras=lista[x]
    return palabra

    
#Progama principal
lista=crearLista()
print(lista)
pribnt("Palabras con caracteres:",mascaracteres(palabras))
